{
  "name": "quran-learning-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && yarn build",
    "start": "cd frontend && yarn start",
    "dev": "concurrently \"cd backend && python server.py\" \"cd frontend && yarn start\"",
    "deploy": "cd frontend && yarn build"
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
